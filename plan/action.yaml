name: plan
description: plan
inputs:
  github_token:
    description: "GitHub Access Token"
    required: false
    default: ${{ github.token }}
runs:
  using: composite
  steps:
    - uses: dw-zenid/tfaction/terraform-plan@v1.4.0-2
      if: env.TFACTION_JOB_TYPE == 'terraform'
      with:
        github_token: ${{inputs.github_token}}

    - uses: dw-zenid/tfaction/tfmigrate-plan@v1.4.0-2
      if: env.TFACTION_JOB_TYPE == 'tfmigrate'
      with:
        github_token: ${{inputs.github_token}}
